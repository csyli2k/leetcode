class Solution:
    def increasingTriplet(self, nums: List[int]) -> bool:
        n1=math.inf
        n2=math.inf
        for a in nums:
            if a <= n1:
                n1=a
            elif a<=n2:
                n2=a
            else:
                return True
        return False