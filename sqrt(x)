class Solution:
    def mySqrt(self, x: int) -> int:
        l=0
        r=x
        while l<r:
            mid=l+(r-l)//2
            temp=mid*mid
            if temp>=x:
                r=mid
            else:
                l=mid+1
        return l if l*l==x else l-1